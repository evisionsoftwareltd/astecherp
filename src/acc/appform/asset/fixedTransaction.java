package acc.appform.asset;

import com.common.share.SessionBean;
import com.vaadin.ui.Window;

public class fixedTransaction extends Window
{
	public fixedTransaction(SessionBean sessionBean)
	{
		System.out.println("fixed Transaction");
		this.setVisible(false);
	}
}
